interface:
  display_name: "Review Content"
  short_description: "QA and fix a specified unit"
  default_prompt: "Use $review-content <unit N> to QA and fix a specified unit against the catalog and EPUB references in this grammar app."
policy:
  allow_implicit_invocation: false
