interface:
  display_name: "Review Content"
  short_description: "Check and fix units or review packs"
  default_prompt: "Use $review-content to compare an existing unit or review pack against the EPUB references and fix inaccuracies in this grammar app."
policy:
  allow_implicit_invocation: false
