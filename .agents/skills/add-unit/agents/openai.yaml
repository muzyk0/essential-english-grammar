interface:
  display_name: "Add Unit"
  short_description: "Create a specific or next grammar unit"
  default_prompt: "Use $add-unit <unit N> to create a specified unit, or $add-unit next to create the next unit for this grammar app."
policy:
  allow_implicit_invocation: false
